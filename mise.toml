[tasks.install]
description = "Build and install the opq binary to OPQ_INSTALL_DIR (default ~/.local/bin)"
run = '''
dir="${OPQ_INSTALL_DIR:-$HOME/.local/bin}"
mkdir -p "$dir"
go build -o "$dir/opq" .
echo "installed opq -> $dir/opq"
'''
